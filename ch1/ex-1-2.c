#include <stdio.h>

int main() { printf("what happen when with that char ? \c \n"); }

// will print "what happen when with that char ? c ".
// which means when you escape a character that you souldn't it is transformed
// to the unescape character.
